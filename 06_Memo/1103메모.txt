타입이 딕셔너러타입이 넘파이로 넘어가면서 바뀌는 거같아서 
스켈레톤 파일을 넘파이파일로 만드는 코드

넘파이 어레이를 딕셔너리로 바꾸는

리스트나 튜플에서만 적용되서
딕셔너리에선 안될거같은데 

이거를 딕셔너리로 바꾸면 이쉐이프를 딕셔너리의 길이로 바꿔서 넣거나 
다른방법을 찾아봐야될거같고

그리고 이데이터 타입이 지금 넘파이 어레이니까 이거를 일단 딕셔너리로 바꾼다음
리스트로 바

딕셔너리가 키랑 value로 이루어짐

딕셔너리를 리스트로 바꾸면 쉐이프를 사용할 수 있긴한데 
지금 딕셔너리 구조에서 인덱스를 스트링으로

리스트에서는 인덱스를 스트링으로 사용할 수 없어서 문제임


스켈레톤좌표를 3차원 RGB이미지로 바꾸는거같고
RGB가 3차원데이터를

키값이 아니면 따로 설정할 수없는 종류중 하나인지 않은가


입력을 넘파이로 넣으면 안되고,
스켈레톤을 넘파이로 바꾼거임

넘파이는 쉐이프값도 안잡혀서
다른형태로 넣어줘야되는거같음



타입이 넘파이 array고, 각 파일의 shape이 ( 25 , 3, n) 형식 즉, 25행 3열 데이터가 다수개 존재